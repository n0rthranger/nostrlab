{
  "name": "nostrlab-cli",
  "version": "0.1.0",
  "description": "CLI companion for NostrLab — interact with Nostr git repos from the terminal",
  "type": "module",
  "bin": {
    "nostrlab": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "nostr-tools": "^2.12.0",
    "websocket-polyfill": "^0.0.3"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  }
}
